<nav class="navbar navbar-expand-lg navbar-dark bg-primary nav-fill sticky-top mb-3">

  <%# ロゴ部分 %>
  <div class="nav-item">
    <a class="navbar-brand logo" href="/"><i class="fas fa-book"></i>&nbsp; 瞬間読書録</a>
  </div>

  <%# メニュー格納時のボタン %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <%# メニュー %>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
   <%= search_form_for @q ,class: "nav-item form-inline mb-2 mb-lg-0" do |f|  %>
      <%= f.search_field :caption_or_user_name_or_user_username_cont, class: "form-control", placeholder: "検索" %>
    <% end %>
    <% if user_signed_in? %>
      <%= link_to edit_user_registration_path, class: "nav-item" do %>
        <i class="far fa-user"></i>
      <% end %>
    <% else %>
      <%= link_to new_user_session_path, class: "nav-item" do %>
        <i class="far fa-user"></i>
      <% end %>
    <% end %>
  </div>

</nav>